# shared-data-access-ddbb

This library contains the DDBB funtionality, mostly Firebase util methods.

## Running unit tests

Run `nx test shared-ddbb` to execute the unit tests via [Jest](https://jestjs.io).
